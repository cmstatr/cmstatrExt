# Print an `average_curve_optim` object

Print an `average_curve_optim` object

## Usage

``` r
# S3 method for class 'average_curve_optim'
print(x, ...)
```

## Arguments

- x:

  an `average_curve_optim` object

- ...:

  not used

## Value

The object passed to this method is invisibly returned (via
`invisible(x)`).

## See also

[`average_curve_optim()`](https://cmstatrExt.cmstatr.net/reference/average_curve_optim.md)
